using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonC : MonoBehaviour
{
    public bool ButtOn = PlayerCam.GButtOn;

    //this will have cube spawner code with it
    //both buttons still need waypoint traveling
    // Update is called once per frame
    void Update()
    {
        


    }
}
